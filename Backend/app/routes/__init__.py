"""
routes -- FastAPI routers.

Each module exposes a `router` object that is mounted in app/main.py.

Modules:
    document_routes.py -- POST /documents/upload, GET /documents, DELETE /documents/{id}
    query_routes.py    -- POST /query, POST /search, POST /extract
"""
